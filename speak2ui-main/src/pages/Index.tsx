
import { Navigate } from "react-router-dom";
import Home from "./Home";

const Index = () => {
  // Render the Home component directly instead of navigating
  return <Home />;
};

export default Index;
